<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>hello <%=name%></h1>
    <p>
      <%= numbers  %>
      <% if(name === "youcef"){%>
        <div>name is youcef</div>
        <% } %>
        <% for( let index = 0; index < numbers.length; index++ ) { %>
            <span style="color: red ;"><%=numbers[index]%></span>
        <% } %>
    </p>
  </body>
</html>
